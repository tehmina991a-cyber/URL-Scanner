{
  "name": "url-threat-scanner",
  "version": "2.0.0",
  "description": "VirusTotal-backed URL threat scanner. Server-side API key, fast submit-and-poll scanning, per-IP + global rate limiting.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  }
}
