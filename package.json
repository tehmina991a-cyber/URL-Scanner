{
  "name": "url-threat-scanner",
  "version": "1.0.0",
  "description": "VirusTotal-backed URL threat scanner with a local proxy to avoid browser CORS limits.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
